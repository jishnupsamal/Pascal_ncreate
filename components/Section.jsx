import React from 'react'
import PropTypes from 'prop-types'

const Section = ({align}) => {
  return (
    <div>Section</div>
  )
}

Section.propTypes = {
    align: PropTypes.string
}

export default Section